services:
  php:
    image: ghcr.io/nayleen/php:${PHP_VERSION:-8.3}
    container_name: codestandard_php
    environment:
      PHP_CS_FIXER_IGNORE_ENV: ${PHP_CS_FIXER_IGNORE_ENV:-false}
      XDEBUG_MODE: "${XDEBUG_MODE:-off}"
    init: true
    network_mode: host
    volumes:
      - ./:/app/src
