services:
  php:
    image: ghcr.io/nayleen/php:${PHP_VERSION:-8.3}
    container_name: codestandard_php
    environment:
      XDEBUG_MODE: "${XDEBUG_MODE:-off}"
    init: true
    network_mode: host
    volumes:
      - ./:/app/src
